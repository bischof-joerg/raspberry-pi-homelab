# Test runner
pytest>=8.0,<9.0

# HTTP client for runtime smoke tests
requests>=2.31,<3.0

# YAML parsing (Alertmanager config.original, config checks)
PyYAML>=6.0,<7.0

# Lint/format (pinned: must match .pre-commit-config.yaml rev)
ruff==0.14.11

# Shell lint (pinned: must match .pre-commit-config.yaml rev)
shellcheck-py==0.10.0.1

# YAML lint (pinned: must match .pre-commit-config.yaml rev)
yamllint==1.35.1

# Optional (nice-to-have if you want to run "pre-commit run" locally from the venv)
pre-commit>=3.7,<4.0
